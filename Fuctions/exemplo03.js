// Function scope

/* let subject = 'creat video';

function createThink(subject){
        subject = 'study'
        return subject */

    // esse subject aqui dentro desse escopo tem um significado (study) ou seja, ele não irá se sobressair na variável (subject creat video) pois nesse caso estamos pasando parâmetros. function createThink(subject).

    // mas se não passarmos nenhum parâmetro: function createThink() ele irá se sobressair.

    // por fim, se não colocarmos o (return) a mesma irá retornar como undefined, pois toda function precisa ter retorno.

//};

/* console.log(subject);
console.log(createThink(subject)); */
